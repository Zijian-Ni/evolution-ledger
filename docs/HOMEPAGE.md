# Homepage card copy

## EN
**Evolution Ledger** — How an AI agent was actually trained.
Append-only, hash-chained audit trail for agent self-evolution: hypothesis → change → eval → decision → rollback. Tamper-evident, offline, zero deps.

## ZH
**进化账本** — 一个 AI Agent 是怎么被调教出来的。
只追加、哈希链防篡改的自我进化审计记录：假设 → 改动 → 评测 → 决策 → 回滚。纯本地、零依赖。

## Resume bullet
Designed an append-only, hash-chained audit ledger that makes AI agent self-modification falsifiable and reversible (Node CLI + static viewer, 0 runtime deps, 10 tests, CI-verified chain integrity).
